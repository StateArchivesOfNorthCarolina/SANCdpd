"""
This module reads the configuration file and sets global data structures for
use by the rest of the SANCdpd package.
"""

import json

# Global variables:
logfilename=""
person_agent_code=""


def read():
    """
    Read config file to initialize global variables.
    If a required config value is missing or invalid, raise an exception.
    """

def validate():
    """Validate the configuration and the database connection."""

    # check the database connection
    # check whether the database appears valid
